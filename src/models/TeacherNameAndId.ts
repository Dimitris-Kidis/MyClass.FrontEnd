export class TeacherNameAndId {
    id: number;
    teacherName: string;

    constructor(
        id: number,
        teacherName: string,
    ) {
        this.id = id;
        this.teacherName = teacherName;
    }
}