export class ClassWithId {
    id: number;
    className: string;

    constructor(
        id: number,
        className: string,
    ) {
        this.id = id;
        this.className = className;
    }
}