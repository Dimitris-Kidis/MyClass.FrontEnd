export class SubjectsWithId {
    id: number;
    subjectName: string;

    constructor(
        id: number,
        subjectName: string,
    ) {
        this.id = id;
        this.subjectName = subjectName;
    }
}